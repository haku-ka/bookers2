


<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Book</h1>


  <% if @book.errors.any? %>
    <%= @book.errors.count %>error prohibited this obj from being saved: <br />
    <% @book.errors.full_messages.each do |message| %>
    <%= message %>
    <% end %>
  <% end %>

  <%= form_with model: @book, url: book_path(@book.id), local:true do |f| %>
  <div class="form-group">
    <label for="book_name">Title</label><br />
      <%= f.text_field :title %>
  </div>

  <div class="form-group">
    <label for="book_Introduction"></label>opinion<br />
      <%= f.text_area :body %>
  </div>

  <div class="form-group">
   <%= f.submit 'Update Book', class: "btn btn-success" %>

   <% end %><br />


<tr>
   <th><%= link_to "Show", book_path %></th>
   ｜
   <th><%= link_to "Back", books_path %></th>
</tr>


  </div>

